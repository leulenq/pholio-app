.pholio-landing-ref
node_modules
client/node_modules
public/dashboard-app
.env
.env.local
.env.live
.env.bak*
client/.env
client/.env.local
dev.sqlite3
pholio.db
uploads/
.netlify
netlify/function-built/
*.log

# macOS / editor noise
.DS_Store
**/.DS_Store

# Superpowers visual brainstorm companion
.superpowers/

# Scratch DBs from integration tests
# Any local sqlite artifact, however it is named. `test-*.sqlite3` alone missed
# `test.sqlite3` (the hyphen), and `dev.sqlite3` missed `dev.sqlite` (the 3) —
# which is how both came to be tracked. Match the extension, not a guess at the
# stem.
*.sqlite3
*.sqlite
test-*.sqlite3

# Comp-card gallery rig artifacts (regenerate with npm run gallery)
gallery/
dev.sqlite3-journal
*.sqlite3-journal
PERSISTENCE_REPORT.md

# local dev/test sqlite artifacts
data/*.sqlite3

# Wallet preview rig: PNGs are committed, the HTML/JSON sheets are regenerated
docs/wallet/previews/index.html
docs/wallet/previews/cases.json
